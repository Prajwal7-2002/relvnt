# Relvnt
ML-powered Instagram reach intelligence.
Detects algorithm shocks 3 weeks early.

## Setup
```bash
pip install -r backend/requirements.txt
cp backend/.env.example backend/.env
```

Fill `.env` with real credentials.

## Run
```bash
cd backend
python main.py
```
